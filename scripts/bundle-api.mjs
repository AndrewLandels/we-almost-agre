import * as esbuild from 'esbuild'

const shared = {
  bundle: true,
  platform: 'node',
  format: 'esm',
  target: 'node20',
  packages: 'external',
  legalComments: 'none',
  banner: {
    js: '// Generated by scripts/bundle-api.mjs from server/handlers. Do not edit by hand.',
  },
}

await esbuild.build({
  ...shared,
  entryPoints: ['server/handlers/quote.ts'],
  outfile: 'api/quote.js',
})

await esbuild.build({
  ...shared,
  entryPoints: ['server/handlers/polymarket.ts'],
  outfile: 'api/polymarket.js',
})
