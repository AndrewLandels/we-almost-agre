# Optional later — not required for the default delayed Yahoo feed.
# Keys belong on the server (Vercel env / `api/quote.ts`), not in VITE_* .
# PRICE_FEED_PROVIDER=yahoo
# FINNHUB_API_KEY=
# TWELVEDATA_API_KEY=
