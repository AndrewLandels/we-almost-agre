# Optional later — not required for the default delayed Yahoo / Nasdaq feeds.
# Keys belong on the server (Vercel env / `api/quote.js`), not in VITE_* .
# PRICE_FEED_PROVIDER=yahoo
# FINNHUB_API_KEY=
# TWELVEDATA_API_KEY=
